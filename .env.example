# Used only by generate.py for local image generation. Never commit a real key.
GOOGLE_API_KEY=replace-with-your-google-ai-studio-key

# Optional sibling vault path for generated image URL write-back.
# SUPE_PINES_VAULT=../supe-pines-vault
